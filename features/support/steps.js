const { Given, When, Then } = require('cucumber')
const { expect } = require('chai')

Given(`an anonymous user`, function () {
})

Given(`A logged in user`, function () {
})

When('the user opens the home page', function () {
})

Then('they can see a new game board', function () {
})
